---
name: Bug report
about: Something in aum/ai-universal-memory doesn't work as expected
title: ""
labels: bug
---

**What did you run?**
The exact command(s), e.g. `npx ai-universal-memory init`.

**What did you expect?**

**What happened instead?**
Paste the full output/error if there is one.

**Environment**
- `ai-universal-memory` version (`npx ai-universal-memory version`):
- Node version (`node -v`):
- OS:
- Engine involved, if relevant (Claude Code / Cursor / Codex / other):

**Relevant memory state, if applicable**
If this is about `.memory/` behaving oddly, paste the relevant bits of
`.memory/state.json` or the last few lines of `.memory/events.jsonl` —
redact anything sensitive first.
